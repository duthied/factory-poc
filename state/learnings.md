# Learnings

**Owner: reviewer (append-only). Read by the writer before every chunk.**

Capture *reusable* guidance that will make future chunks pass on the first try —
recurring mistakes, spec nuances that were missed, style pitfalls. **Not**
one-off nitpicks about a single chunk (those go in `review-log.md`).

Keep each learning short and imperative so the writer can act on it.

Entry format:

```
- [YYYY-MM-DD] (from chunk-NN) <the reusable lesson, phrased as a rule>
```

> **Promoted to the spec on 2026-08-30 (Spec-Version 2) and truncated:** the
> fixed-timeline learning became §7 canon, and the impersonal-"you" learning was
> folded into §3. When a learning proves durable, promote it into `spec.md` (a
> rule or a canon fact), bump the Spec-Version, and clear it from here so this
> file stays a short list of *active, not-yet-promoted* guidance.

---

<!-- learnings appended below -->
